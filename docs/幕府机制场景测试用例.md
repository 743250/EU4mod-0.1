# 幕府机制场景检查

本文件只记录实现完成后的关键回归检查，以真实 EU4 脚本链和进游戏日志为准。

## 静态结构

步骤：

- 我运行 `python tools/check_static_regressions.py`。
- 我运行 `python tools/check_script_smells.py`。

预期：

- 政府列表保留目标版本原版条目，只额外登记一次 `mf_daimyo` 和一次 `mf_retainer_daimyo`。
- 京都易主后，玩家新幕府由本模组 `on_province_owner_change` 安排一天后的 `mf_daimyo.13`，AI 新幕府由专用决议完成交接补充；两者都不重复调用 `japan.1`。
- `zhengyidajiangjun` 只在开局初始化、玩家交接事件和 AI 交接决议中写入。
- 不存在 `on_create_vassal`、月脉冲或通用 normalize effect 批量整理属国关系。
- 花括号全部平衡。

## 原版开局

步骤：

- 我输入：仅启用本模组和其声明依赖的中文模组，完全退出并重新启动 EU4。
- 我输入：以 1444 年日本剧本开局。
- 我查询：足利是否拥有原版 `shogunate`，直属大名是否仍为原版 `daimyo_vassal`。
- 我查询：`zhengyidajiangjun`、`shogun_value`、天皇目标和虾夷目标。
- 我查询：原版政府机制中的驱逐浪人、参勤交代和刀狩令是否仍显示。
- 我查询：统御入口、统御窗口和直属大名盾徽是否显示。

预期：

- 返回足利是原版幕府将军。
- 返回直属大名仍走原版幕府关系。
- `zhengyidajiangjun` 返回足利，统御返回 `50.00`；天皇和虾夷目标都返回未设置。
- 返回三项原版幕府互动不再显示，自定义统御 UI 与五项御令正常存在。
- 直属大名盾徽在开局初始化后直接显示，不需要先点击刷新按钮。
- 自定义 UI 不改变国家改革或属国关系。

## 德川独特理念加载

步骤：

- 我输入：仅启用本模组和其声明依赖的中文模组，完全退出并重新启动 EU4。
- 我输入：以 1444 年 TKG 德川开局。
- 我查询：德川当前国家理念组 key，以及第一条理念 key。
- 我查询：`common/ideas` 文件排序中 `000_mf_country_ideas.txt` 是否位于原版 `00_country_ideas.txt` 之前，并检查 `error.log`。

预期：

- 返回 `mf_TKG_ideas`，第一条理念返回 `mf_tkg_mikawa_bushi`，不返回原版 `TKG_ideas`。
- 模组理念文件排在原版独特国家理念文件之前；本地化没有裸 key。
- `error.log` 不出现理念组解析错误、未知理念 key 或本地化缺失错误。

## 京都易主与将军缓存交接

步骤：

- 我输入：建立旧幕府 `S`、旧幕府直属普通大名 `D`、谱代 `F`，以及拥有家臣 `B` 的大名 `A`。
- 我输入：先由玩家控制 `A` 取得京都并获得 `shogunate`，再用 AI 控制的 `A` 重复同一场景。
- 我查询：玩家场景中京都易主当日和次日的 `global event_target:zhengyidajiangjun`；AI 场景中查询 `mf_ai_complete_shogunate_transfer` 执行前后缓存指向。
- 我查询：原版 `japan.1` 与玩家隐藏事件或 AI 决议处理后的 `D`、`F`、`B` 宗主、属国类型和政府改革。
- 我查询：下一个月度脉冲后 `zhengyidajiangjun` 指向谁。
- 我查询：新幕府的 `shogun_value`。

预期：

- 京都易主当日由原版 `japan.1` 完成幕府身份和普通大名转挂；玩家场景次日、AI 场景执行专用决议后，`zhengyidajiangjun` 从 `S` 切换为新幕府 `A`。
- `D`、`F` 和 `B` 的宗主都返回 `A`，属国类型都返回 `daimyo_vassal`，政府改革都返回 `daimyo`。
- `D` 由原版 `japan.1` 转挂；本模组只补充旧幕府直属谱代、异常家臣与新幕府原有家臣。
- 新幕府统御初始化为 `50`，但统御值不反过来决定谁是幕府。
- 交接当日不运行 `bl_mp_effect`；第一次月度变化发生在下一个月脉冲。
- 玩家控制 `A` 时由次日隐藏事件完成上述补充；AI 控制 `A` 时由 `mf_ai_complete_shogunate_transfer` 决议完成相同结果。

## 独立战争未取得京都

步骤：

- 我输入：建立幕府 `S`、直属大名 `A` 和 `A` 的家臣 `B`。
- 我输入：让 `A` 赢得独立战争，但不取得京都。
- 我查询：`A`、`B` 的政府改革，以及 `B` 的宗主和属国类型。

预期：

- `A` 返回 `indep_daimyo`。
- `B` 的宗主仍返回 `A`，属国类型仍返回 `mf_retainer_daimyo_vassal`，政府改革仍返回 `mf_retainer_daimyo`。
- `zhengyidajiangjun` 仍指向拥有 `shogunate` 的 `S`。

## 普通大名、家臣与谱代

步骤：

- 我输入：幕府建立普通直属大名 `A`，`A` 建立家臣 `B`，幕府建立谱代 `F`；另建立独立大名 `I`，由 `I` 建立家臣 `C`。
- 我查询：`A`、`B`、`F`、`I`、`C` 的宗主、属国类型和第一层政府改革。
- 我查询：普通直属大名 `A` 的陆军与海军规模上限分项。
- 我输入：记录 `B`、`C` 的正统性并让场景稳定运行十二个月。
- 我再次查询五国关系、第一层政府改革、正统性和 `flavor_jap.57` 是否出现，并检查 `error.log`。

预期：

- `A` 是幕府的 `daimyo_vassal`。
- `A` 只返回一次“大名领国”的固定陆军与海军上限惩罚，不再同时返回原版“属国”的同值惩罚。
- `A` 使用 `daimyo`，`B`、`C` 使用 `mf_retainer_daimyo`，`F` 使用 `mf_daimyo`，`I` 使用 `indep_daimyo`。
- `B` 是 `A` 的 `mf_retainer_daimyo_vassal`，不会被原版机制转挂给幕府。
- `F` 是幕府的 `mf_daimyo_vassal` 并使用 `mf_daimyo`。
- `C` 是独立大名 `I` 的 `mf_retainer_daimyo_vassal`。
- `B`、`C` 返回不能宣战、发动独立战争、结盟、联姻、设置宿敌或建立属国，但会随各自宗主参加战争。
- `B`、`C` 的领土分别计入 `A`、`I` 的边界。
- 十二个月内不批量重写五国的宗主、属国类型或政府改革，`B`、`C` 不出现 `flavor_jap.57`，也不因该事件损失 `25` 正统性。
- `error.log` 不返回未知属国类型、未知政府改革、无效潜在宗主或 `mf_retainer_daimyo_vassal`、`mf_retainer_daimyo` 解析错误。

## 战国收服

步骤：

- 我输入：建立幕府 `S`、直属大名 `A`、`B`、`A` 的家臣 `D` 和 `B` 的家臣 `C`；使 `A` 与 `B` 的直属领土不接壤，只有 `A` 或 `D` 的领土与 `C` 接壤。
- 我查询：`A` 对宗主大名 `B` 的宣战界面中“战国收服”的数量和战争目标；再查询 `A` 是否能以家臣 `C` 为战国收服目标或直接向 `C` 宣战。
- 我查询：`C`、`D` 是否能主动宣战、发动独立战争或建立外交关系。
- 我输入：保持双方关系不变并运行两个月，检查战争借口提示。
- 我输入：`A` 对 `B` 使用战国收服，查询参战国家；随后击败 `B` 并执行 `po_mf_subjugate_daimyo`。
- 我查询：`B`、`C` 的宗主、属国类型和政府改革。
- 我输入：让幕府 `S` 作为 `mf_sengoku_subjugation` 的胜方向独立大名 `I` 和谈，查询 `po_mf_subjugate_daimyo` 是否可见、可执行；再查询 `I` 是否会成为 `S` 的家臣。

预期：

- `B` 返回宗主 `A`、属国类型 `mf_retainer_daimyo_vassal`、政府改革 `mf_retainer_daimyo`。
- 宣战界面在自定义条件成立时返回本模组的“战国收服”，战争目标返回 `mf_sengoku_subjugation`；如果同时满足原版条件，排他 CB 应压过原版条目，不再并列显示原版“战国收服”；两个月内不出现失去后重新获得该借口的提示。
- 普通大名、谱代大名和独立大名通过敌方家臣领土与合法大名宗主接壤时，都返回对该宗主拥有 `cb_mf_sengoku`；家臣返回没有，家臣也不是该借口的目标。直接选择家臣 `C` 时返回不能宣战，并提示改向宗主大名 `B` 宣战。
- 战争返回 `A`、`D` 对抗 `B`、`C`，幕府 `S` 不参战；宣战对象和防守方大名战争领袖都是 `B`，不是家臣 `C`。
- `C`、`D` 返回不能主动宣战、发动独立战争、结盟、联姻或设置宿敌。
- `C` 返回宗主 `A`、属国类型 `mf_retainer_daimyo_vassal`、政府改革 `mf_retainer_daimyo`。
- `B`、`C` 都是 `A` 的直属家臣，不产生第三级属国。
- 和平条款不割地、不吞并国家，只收服 `B` 并把 `B` 原有直属家臣直接转交给 `A`；转交过程中 `C` 不获得独立，也不成为幕府直属家臣。
- 幕府 `S` 返回看不到且不能执行 `po_mf_subjugate_daimyo`；`I` 不会成为 `S` 的 `mf_retainer_daimyo_vassal`。

## AI 大名分封旧家

步骤：

- 我输入：建立幕府 `S`、直属 AI 大名 `A`，并分别让 `S`、`A` 持有一个拥有已灭亡日本文化组国家核心的省份。
- 我查询：`mf_release_retainer_daimyo_from_land` 是否可执行。
- 我输入：让 `A` 处于战争，再次查询决议；随后恢复和平并执行决议。
- 我输入：把另一个 AI 国家设为 `mf_retainer_daimyo_vassal`，查询其是否能执行分封决议或获得 `cb_mf_sengoku`。
- 我查询：由 `A` 分封的 `B` 和由 `S` 分封的 `C` 是否恢复、宗主、属国类型、政府改革和对分封者的好感修正。
- 我输入：完成武家诸法，再次建立同样的灭亡旧家省份条件并查询决议。

预期：

- 和平且武家诸法尚未完成时，返回决议可执行。
- 战争期间返回不可执行。
- `B` 返回宗主 `A`、属国类型 `mf_retainer_daimyo_vassal`、政府改革 `mf_retainer_daimyo`。
- `C` 返回宗主 `S`、属国类型 `daimyo_vassal`、政府改革 `daimyo`。
- `B`、`C` 对各自分封者都返回原版 `released_vassal` 好感，并继承各自分封者的政府改革进度。
- 决议先割让目标省份使旧家复国，再按执行者身份建立家臣或幕府直属普通大名；不修改幕府统御。
- 武家诸法完成后返回决议不再出现。
- 家臣返回不能执行分封决议，也不能使用战国收服继续建立第三级属国。

## 拒绝参勤交代

步骤：

- 我输入：记录幕府当前统御值，让直属大名在 `mf_daimyo.10` 中选择拒绝征召。
- 我查询：该大名的威望、`mf_sankin_kotai_refused` 和幕府统御值。

预期：

- 该大名获得 `10` 威望和持续一年的 `mf_sankin_kotai_refused`。
- 幕府统御值与事件前一致。

## 家臣拒令后的家内绝罚

步骤：

- 我输入：建立幕府 `S`、直属普通大名 `A` 和 `A` 的家臣 `B`。
- 我输入：使 `B` 拒绝一项能产生拒命标记的幕府命令，然后查询 `S` 能否对宗主大名 `A` 执行绝罚，以及能否直接绝罚 `B`。
- 我输入：由 `S` 对 `A` 执行绝罚，查询 `A` 是否获得 `mf_shogunate_excommunication`，`S` 是否获得以 `A` 为目标的 `cb_mf_shogunate_excommunication`。
- 我输入：`S` 使用绝罚 CB 对 `A` 宣战并打开和谈界面，查询参战国家。
- 我查询：`po_mf_enforce_shogunate_obedience` 是否可见，执行后 `A` 的绝罚与独立倾向，以及 `B` 的拒命标记如何变化。

预期：

- `B` 的拒命状态使 `S` 返回可以绝罚宗主大名 `A`，但不能把家臣 `B` 作为绝罚或战争目标。
- 返回绝罚修正位于 `A`，CB 目标返回 `A`；战争由 `S` 对抗 `A` 与随宗主参战的 `B`。
- 返回强制服从和平条款可见；和谈后 `A` 的绝罚与 `B` 的拒命标记被清理，`A` 独立倾向降低，幕府获得对应统御。

## 归还非法领地

步骤：

- 我输入：完成归还非法领地改革并登记幕府、直属大名和下一层家臣领地。
- 我输入：让体系外国家 `X` 持有一个已登记省份，`X` 在该省没有核心，该省拥有现存幕府体系国家 `A` 的核心；对 `X` 使用归还非法领地外交行动并选择同意。
- 我查询：本次归还省份数量、归还对象，以及 `A` 的宗主与属国类型。
- 我输入：分别重建只有已灭亡国家 `B` 核心、以及没有任何现存幕府体系国家核心的已登记省份，再让 `X` 同意归还。
- 我查询：两种场景的省份所有者；随后由幕府运行释放大名决议，并查询被恢复国家的宗主与属国类型。
- 我输入：让幕府体系内国家持有一个已登记且没有本国核心的省份，查询同一外交行动是否可用。
- 我重新建立相同场景，对 `X` 使用外交行动并选择拒绝。
- 我查询：`X` 持有的全部符合条件省份是否拥有 `mf_unlawful_daimyo_land`，幕府是否获得归还领地 CB。
- 我输入：幕府使用归还领地 CB 战胜 `X`，选择 `po_mf_return_shogunate_provinces`。
- 我查询：有现存幕府体系核心国、只有灭亡国家核心以及没有幕府体系核心的非法省份所有者，并检查非法领土修正。

预期：

- 同意时只归还一个符合条件的省份。
- 存在现存幕府体系核心国时，省份返回该国；接收国不是当前幕府直属属国时，返回其宗主为幕府、属国类型为 `daimyo_vassal`。
- 只有灭亡国家核心或没有现存幕府体系核心国时，省份返回幕府；外交行动不恢复灭亡国家。幕府运行释放大名决议后，恢复国家返回宗主为幕府、属国类型为 `daimyo_vassal`。
- 目标位于幕府体系内或体系外时使用相同的省份资格条件。
- 拒绝时所有符合条件的省份都返回拥有 `mf_unlawful_daimyo_land`，不是只标记一个随机省份。
- 幕府获得可用于这些非法领土的归还领地 CB。
- 和平界面使用原版 `po_return_cores` 归还现存幕府体系核心省份，使用幕府专用条款归还没有现存体系核心的省份；被处分省份的非法领土修正被清理，不能用专用条款把省份割给第三国。
- AI 接受理由返回基础抗拒 `-75`、体系外国抗拒 `-50`、幕府外交接受度、目标对幕府的意见乘以 `0.2`、幕府外交声誉乘以 `2`、幕府负威望乘以 `0.2`、连续军力与纪律威慑、敌对态度和历史宿敌；所有连续分项均返回整数。
- 目标对幕府态度为敌对、震怒或宿敌时返回 `-1000`；此前已有 `opinion_mf_demanded_unlawful_land` 时返回 `-100`。接受或拒绝后再次查询，均返回此前要求惩罚。

## 归还家臣国家

步骤：

- 我输入：建立幕府 `S`、直属大名 `A`、`A` 的家臣 `B` 和另一直属大名 `C`，使各国处于和平。
- 我输入：记录 `A`、`C` 对 `S` 的好感，以 `B` 为目标执行 `mf_transfer_illegal_subject`。
- 我查询：`mf_transfer_illegal_subject` 的各项接受理由分别读取哪个国家的数据。
- 我输入：分别让行动接受和拒绝；AI 场景再分别令总接受值高于零和低于零。
- 我查询：两种结果下 `B` 的宗主、属国类型和政府改革，并再次查询 `A`、`C` 对 `S` 的好感。
- 我检查：执行两种选项后生成的新 `error.log`。

预期：

- 返回外交目标仍是具体家臣 `B`，行动使用 `require_acceptance = yes`。
- AI 接受值返回 `-50` 基础抗拒、`A` 对 `S` 的意见乘以 `0.2`、幕府外交接受度、幕府外交声誉乘以 `2`、幕府负威望乘以 `0.2`、幕府与 `A` 的连续军力和纪律差、`B` 的总发展度乘以 `-0.5`，以及此前要求、态度和历史宿敌分项。除发展度读取 `B` 外，所有目标侧政治与军事实力数据都返回读取原宗主 `A`，不能读取 `B`。
- 行动接受时，`B` 返回宗主 `S`、属国类型 `daimyo_vassal`、政府改革 `daimyo`，幕府御统增加 `2`。
- 行动拒绝时，`B` 仍返回宗主 `A`、属国类型 `mf_retainer_daimyo_vassal`，幕府御统不变。
- 无论接受或拒绝，`A` 对 `S` 返回 `opinion_mf_returned_country` 的 `-50`，`C` 对 `S` 返回 `opinion_mf_shogunate_intervened_in_daimyo_house` 的 `-10`；`B` 不获得其他大名的群体负面关系。
- `error.log` 不出现 `mf_transfer_illegal_subject`、`FROM.overlord` 作用域、接受度分项或未知效果错误。

## 谱代任命接受度

步骤：

- 我输入：完成谱代改革，建立幕府 `S` 与两个发展度、意见、信任相同但距离不同的直属普通大名 `A`、`B`。
- 我查询：`A`、`B` 的 `mf_convert_daimyo` 接受度分项，并令 `A` 接受、`B` 拒绝。
- 我查询：`A`、`B` 的属国类型和政府改革，并检查新 `error.log`。

预期：

- 返回谱代任命要求目标同意，不再由幕府单方面转换；目标不需要与幕府直接接壤。
- 接受度返回目标对幕府的意见乘以 `0.2`、超过 `50` 的信任乘以 `0.4`、幕府外交接受度、目标发展度乘以 `-0.5`、边界距离乘以 `-0.5`、幕府外交声誉乘以 `2`，以及目标态度和历史宿敌分项；不返回基础抗拒。
- 在其他条件相同时，距离更远者返回更低接受度；边界距离每增加 `1`，接受度降低 `0.5`。
- `A` 接受后返回 `mf_daimyo_vassal` 和 `mf_daimyo`；`B` 拒绝后关系不变。

## 天皇外交目标

步骤：

- 我输入：完成禁中并公改革，先查询天皇目标。
- 我输入：由幕府 `S` 对直属大名 `T` 使用宣下奉行外交行动。
- 我查询：天皇目标及 `T` 的 `mf_tianhuang_target`。
- 我输入：使 `T` 不再是 `S` 的属国，再执行一次月度脉冲。

预期：

- 改革完成后、外交行动执行前，天皇目标返回未设置。
- 外交行动执行后，天皇目标返回 `T`，`T` 返回拥有 `mf_tianhuang_target`。
- `T` 不再是幕府属国后，目标和 flag 被清理；月脉冲不会随机指定新的天皇。

## 虾夷持续战争理由

步骤：

- 我输入：完成虾夷改革，先查询虾夷目标，再由幕府 `S` 使用狩野虾夷外交行动指定体系外国家 `X`。
- 我查询：`S` 是否持续拥有对 `X` 的 `cb_mf_xiayi_campaign`。
- 我输入：和平状态执行一次月度脉冲，再让 `S` 与 `X` 交战并执行一次月度脉冲。
- 我查询：两次 `shogun_monthly_value_10`。
- 我输入：等待十年冷却后把虾夷目标从 `X` 更换为合法国家 `Y`。
- 我查询：`S` 对 `X` 和 `Y` 的虾夷 CB。
- 我输入：使 `Y` 灭亡或进入幕府体系，再执行一次月度脉冲。
- 我查询：虾夷目标缓存、目标 flag、CB 和月度虾夷统御分项。

预期：

- 改革完成后、外交行动执行前，虾夷目标返回未设置；行动执行后返回 `X`。
- `X` 合法作为虾夷目标期间，CB 持续存在，不依赖手动添加的十年期限。
- 和平时虾夷统御分项返回 `0.00`；幕府与合法虾夷交战时返回 `0.02`。
- 更换后对 `X` 的虾夷 CB 自动消失，对 `Y` 的虾夷 CB 自动出现。
- `Y` 失去合法虾夷身份后，缓存和 flag 被清理，CB 与月度 `+0.02` 自动消失。
- 玩家和 AI 都能在十年冷却后重新指定合法目标。

## 汉化与统御 UI

步骤：

- 我输入：确认启动器同时启用 `Chinese Language Mod for 1.37` 和本模组。
- 我输入：完全重启游戏后打开幕府界面。
- 我查询：中文字符、统御数值、御令按钮和关闭按钮。

预期：

- 中文字体由依赖模组提供，本模组的 `topbar.gui` 保留统御窗口。
- 统御数值可见，按钮可点击，关闭后窗口隐藏。
- UI 点击只改变界面 flag，不写入幕府将军缓存。

## 错误日志

步骤：

- 我输入：完成开局、打开 UI、执行一次月度脉冲和一次京都易主后退出游戏。
- 我查询：`Documents/Paradox Interactive/Europa Universalis IV/logs/error.log`。

预期：

- 不出现原版改革重复定义、原版事件重复 id、未知属国类型、未定义 `zhengyidajiangjun`、花括号或 GUI 解析错误。
- DDS 压缩格式警告单独记录，不与脚本生命周期问题混为一谈。
