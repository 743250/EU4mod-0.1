# 幕府机制场景测试用例

本文是 Codex 自己执行的测试操作手册。测试不能写成“玩家进游戏观察”，必须对应到本地逻辑模拟器、静态检查脚本或可读取文件断言。

基础命令：
- 逻辑模拟器：`python tools/mf_shogun_logic_simulator.py`
- 静态回归：`python tools/check_static_regressions.py`
- 任务布局：`python tools/check_mission_layout.py missions\mf_shogun_missions.txt --map --svg tools\mf_shogun_missions_layout.svg`

测试分级：
- 我已经写好的模拟器函数，必须运行并通过。
- 我已经写好的静态检查，必须运行并通过。
- 我尚未覆盖的机制，写成“我待补函数”，不作为当前通过标准。

## 快速回归组合

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我运行 `python tools/check_static_regressions.py`。
- 我修改任务坐标时，运行任务布局工具。

预期：
- 我看到所有模拟器函数输出 `PASS`。
- 我看到所有静态检查输出 `PASS`。
- 我看到任务布局工具不报硬错误。

重点检查：
- 不再把手动进游戏观察当成唯一测试。
- 我新增机制时，必须优先补模拟器函数或静态断言。

## 大名战争与属国层级

### 1. 外样大名私战收服外样大名

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：建立 `SHO`、`A`、`B`。
- 我输入：`SHO` 拥有 `shogunate`，`A` 和 `B` 都是 `SHO` 的 `daimyo_vassal`。
- 我输入：`A` 使用战国私战对 `B` 宣战。
- 我输入分支一：`A` 使用割地和平吞并 `B` 的全部省份。
- 我输入分支二：`A` 使用 `po_mf_subjugate_daimyo` 收服 `B`。

预期：
- 吞并分支中，`B` 灭亡，`A` 获得 `B` 全部省份。
- 吞并分支中，`A` 仍是 `SHO` 的 `daimyo_vassal`。
- 收服分支中，`B` 存在，宗主是 `A`。
- 收服分支中，`B` 是 `mf_retainer_daimyo_vassal`。
- 收服分支中，`B` 拥有 `daimyo` 政府改革。
- 收服分支中，模组覆盖文件里不存在 `flavor_jap.57` 事件定义。
- 收服分支中，幕府统御不变化。

重点检查：
- `test_private_war_annex` 必须通过。
- `test_private_war_subjugation` 必须通过。
- 我检查 `po_mf_subjugate_daimyo` 不调用 `mf_change_shogun_value`。

### 2. 外样大名释放已灭亡旧家为陪臣

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_release_extinct_retainer`。
- 我输入：`A` 先吞并 `B`。
- 我输入：`A` 释放已灭亡大名 `B2`。

预期：
- `B2` 的宗主是 `A`。
- `B2` 是 `mf_retainer_daimyo_vassal`。
- `B2` 拥有 `daimyo` 政府改革。
- `A` 仍是 `SHO` 的 `daimyo_vassal`。
- 模组覆盖文件里不存在 `flavor_jap.57` 事件定义。

重点检查：
- `test_release_extinct_retainer` 必须通过。
- 我后续若恢复分封决议入口，补静态检查确认只筛已灭亡核心国。

### 3. 谱代大名被普通大名收服

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_fudai_subjugated_by_outside_daimyo`。
- 我输入：读取 `common/peace_treaties/00_mf_subjugate_daimyo.txt`。
- 我查询：`po_mf_subjugate_daimyo` 是否调用 `mf_rehome_daimyo_subjects_effect`。
- 我输入：读取 `common/scripted_effects/mf_daimyo_efx.txt`。
- 我查询：`mf_rehome_daimyo_subjects_effect` 是否遍历被收服大名的直属属国。
- 我查询：该 effect 是否覆盖 `daimyo_vassal`、`mf_daimyo_vassal` 和 `mf_retainer_daimyo_vassal`。
- 我查询：该 effect 是否把被收服大名的属国转挂到新宗主，并在新宗主不是幕府时改成 `mf_retainer_daimyo_vassal`。
- 我输入：按 `po_mf_subjugate_daimyo` 的语句顺序执行和平效果：保存 `mf_daimyo_rehome_lord`、创建 `B` 为 `A` 的陪臣、在 `B` 作用域执行属国转移、规范 `B` 政府改革、执行属国层级整理。
- 我输入：把 `B` 设为 `SHO` 的 `mf_daimyo_vassal`。
- 我输入：建立 `C`，并把 `C` 设为 `B` 的 `mf_retainer_daimyo_vassal`。
- 我输入：外样大名 `A` 使用 `po_mf_subjugate_daimyo` 收服 `B`。
- 我输入：查询 `B` 目前的宗主。
- 我输入：查询 `B` 目前的属国类型。
- 我输入：查询 `B` 目前的政府改革。
- 我输入：查询 `C` 目前的宗主。
- 我输入：查询 `C` 目前的属国类型。
- 我输入：查询 `C` 目前的政府改革。

预期：
- 查询 `B` 目前的宗主，返回 `A`。
- 查询 `B` 目前的属国类型，返回 `mf_retainer_daimyo_vassal`。
- 查询 `B` 目前的政府改革，返回 `daimyo`。
- 查询 `B` 是否仍拥有 `mf_daimyo`，返回否。
- 查询 `C` 目前的宗主，返回 `A`。
- 查询 `C` 目前的属国类型，返回 `mf_retainer_daimyo_vassal`。
- 查询 `C` 目前的政府改革，返回 `daimyo`。

重点检查：
- 我输入的和平条款必须是 mod 真实存在的 `po_mf_subjugate_daimyo`。
- 我检查执行的效果顺序必须和 `po_mf_subjugate_daimyo` 内真实脚本一致。
- 查询 `B` 和 `C` 的结果必须同时正确；只验证 `B` 不算通过。
- 我检查使用的属国类型和政府改革必须全部来自 mod 或原版真实 key。

## 复国与领地归还

### 4. 幕府从直辖地重新分封灭亡大名

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_direct_shogun_release`。
- 我输入：让 `SHO` 持有旧家省份 `old1`。
- 我输入：`SHO` 释放 `OLD` 为幕府直属大名。

预期：
- `OLD` 的宗主是 `SHO`。
- `OLD` 是 `daimyo_vassal`。
- `OLD` 拥有 `daimyo` 政府改革。
- 幕府统御增加。

重点检查：
- `test_direct_shogun_release` 必须通过。
- 我检查静态脚本或后续模拟器不得依赖 `previous_owner` 判断旧家。

### 5. 要求归还领地接受

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_return_land_accept_and_decline` 的接受分支。
- 我输入：让 `A` 持有旧家省份 `old1`。
- 我输入：`A` 接受幕府要求归还领地。

预期：
- `OLD` 被恢复。
- `OLD` 的宗主是 `SHO`。
- `OLD` 是 `daimyo_vassal`。
- `A` 获得 `mf_recently_returned_daimyo_land`。
- 幕府统御增加。

重点检查：
- 接受分支必须通过。
- 后续静态检查应确认实际脚本通过 `mf_pick_returnable_daimyo_land_effect` 选目标。

### 6. 要求归还领地拒绝

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_return_land_accept_and_decline` 的拒绝分支。
- 我输入：让 `A` 拒绝归还 `old1`。

预期：
- `A` 获得 `mf_recently_refused_return_daimyo_land`。
- `A` 获得 `mf_unlawful_daimyo_land`。
- `SHO` 获得 `cb_mf_return_daimyo_land`。
- 幕府统御下降。

重点检查：
- 拒绝分支必须通过。
- 拒绝逻辑不能直接吞并或隐藏处理大名。

### 6A. 训诫与赦免大名

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_lecture_grace_enforce_partition_executor` 的训诫赦免部分。
- 我输入：`SHO` 对 `A` 使用训诫大名。
- 我输入：`SHO` 再对 `A` 使用赦免大名。

预期：
- 训诫后，`A` 获得 `mf_recently_lectured_by_shogun`。
- 训诫后，`A` 获得 `mf_shogunate_lecture`。
- 赦免后，`A` 不再拥有 `mf_shogunate_lecture`。
- 赦免后，`A` 获得 `mf_shogunate_grace`。
- 两个动作都消耗幕府统御。

重点检查：
- 训诫和赦免不创建属国、不改政府改革、不转挂国家。

### 6B. 强制和平接受与拒绝

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_lecture_grace_enforce_partition_executor` 的强制和平部分。
- 我输入：`A` 接受幕府强制和平。
- 我输入：`A` 拒绝幕府强制和平。

预期：
- 接受分支按真实脚本扣除幕府统御。
- 拒绝分支中，`A` 获得 `mf_refused_enforce_daimyo_peace`。
- 拒绝分支按真实脚本扣除幕府统御。

重点检查：
- 强制和平拒绝不额外添加普通 CB，而是进入防御战争逻辑。

### 6C. 强制分割外交行动与和平条款

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_lecture_grace_enforce_partition_executor` 的强制分割部分。
- 我输入：`A` 拒绝强制分割。
- 我输入：幕府使用 `po_mf_partition_daimyo` 对 `A` 执行强制分割和平效果。

预期：
- 拒绝后，`SHO` 获得 `cb_mf_partition_daimyo`。
- 和平效果后，旧家 `OLD` 被恢复。
- `OLD` 的宗主是 `SHO`。
- `OLD` 是 `daimyo_vassal`。

重点检查：
- 分割逻辑恢复旧家，不直接吞并 `A`。

### 6D. 任命执行国外交行动与和平条款

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_lecture_grace_enforce_partition_executor` 的任命执行国部分。
- 我输入：幕府任命 `A` 为执行国。
- 我输入：`B` 拒绝执行国任命。

预期：
- `A` 变为 `mf_daimyo_vassal`。
- `A` 拥有 `mf_daimyo` 政府改革。
- `A` 获得 `mf_shogunate_executor`。
- `B` 拒绝后，`B` 获得 `mf_refused_executor_appointment`。
- `B` 拒绝后，`SHO` 获得 `cb_mf_enforce_executor_service`。

重点检查：
- 同一时间只能存在一个执行国。
- 检查现任执行国时直接使用 `NOT = { any_subject_country = { ... } }`，不使用“计数至少一国后再取反”的绕行写法。

### 6E. 幕府治理宣战借口与虾夷目标并存

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我运行 `python tools/check_static_regressions.py`。
- 我输入：建立幕府 `SHO`、直属大名 `A` 和体系外国 `XIA`。
- 我输入：`SHO` 将 `XIA` 指定为当前虾夷征讨目标。
- 我查询：`SHO` 是否立即拥有针对 `XIA` 的 `cb_mf_xiayi_campaign`。
- 我输入：`A` 拒绝归还非法领地。
- 我查询：`SHO` 是否拥有针对 `A` 的 `cb_mf_return_daimyo_land`。
- 我输入：`SHO` 绝罚 `A`。
- 我查询：`SHO` 是否拥有针对 `A` 的 `cb_mf_shogunate_excommunication`。
- 我输入：由 `NEW` 接任幕府，并保留当前虾夷目标 `XIA`。
- 我查询：`NEW` 是否拥有针对 `XIA` 的 `cb_mf_xiayi_campaign`。
- 我查询：`po_mf_partition_daimyo`、`po_mf_enforce_executor_appointment` 和 `po_mf_enforce_shogunate_obedience` 是否分别由独立和平条款文件定义。

预期：
- 指定虾夷后，立即返回 `SHO` 拥有针对 `XIA` 的 `cb_mf_xiayi_campaign`。
- 大名拒绝归还领地后，返回 `SHO` 拥有针对 `A` 的 `cb_mf_return_daimyo_land`。
- 绝罚后，返回 `SHO` 拥有针对 `A` 的 `cb_mf_shogunate_excommunication`。
- 幕府转移后，返回 `NEW` 拥有针对 `XIA` 的 `cb_mf_xiayi_campaign`。
- 三个治理和平条款都能在战争目标加载前被 EU4 单独注册。

重点检查：
- 宣战借口必须记录持有国、目标国和真实 CB key，不能只在模拟器里保存一个无目标的字符串。
- 虾夷宣战借口不能依赖月初条件刷新；指定目标和幕府转移时都要明确授予。
- 绝罚、归还领地和虾夷宣战借口互不覆盖，可以同时存在。
- 新 `error.log` 不应再出现三个 `Unknown peace option`。

### 6F. 维新改革与幕府任务树

步骤：
- 我运行 `python tools/check_static_regressions.py`。
- 我输入：读取 `common/custom_gui/shogun_gui.txt` 中维新页面按钮、完成图标、滚轮图标和上下箭头的页面 flag 条件。
- 我查询：同时要求不存在 `weixin_page_2` 和 `weixin_page_3` 时，是否统一写在同一个 `NOT` 块内。
- 我查询：御令和维新按钮的默认国家作用域中，是否直接写 `tag = event_target:zhengyidajiangjun`。
- 我查询：是否还存在只为检查将军 tag 而包裹的 `ROOT = { ... }`。
- 我修改任务坐标时，运行 `python tools/check_mission_layout.py missions\mf_shogun_missions.txt --map --svg tools\mf_shogun_missions_layout.svg`。
- 我待补函数：`test_weixin_reform_unlocks`。
- 我待补函数：`test_shogun_mission_unlocks_diplomatic_actions`。

预期：
- 我看到静态检查通过。
- 查询维新第一页的页面条件，返回 `NOT = { has_country_flag = weixin_page_2 has_country_flag = weixin_page_3 }`，不返回两个相邻的单条件 `NOT` 块。
- 查询御令和维新按钮，返回直接 `tag = event_target:zhengyidajiangjun`，不返回 `ROOT = { tag = event_target:zhengyidajiangjun }`。
- 我看到任务布局工具通过。
- 我后续补模拟器时，应验证：维新改革不互斥，任务奖励解锁外交行动，改革和任务不重复发放同一奖励。

重点检查：
- 不把“打开面板看一眼”写成测试通过条件。
- 页面 flag 的等价条件由静态检查覆盖；实际按钮和滚轮显示仍需进游戏确认。
- `custom_button` 默认国家作用域中的本国条件直接写，不用 `ROOT` 重入同一作用域。

### 6G. 土著移除决议不再出现

步骤：
- 我运行 `python tools/check_static_regressions.py`。
- 我扫描 `decisions` 目录。

预期：
- `decisions/mf_remove_natives.txt` 不存在。
- 决议目录中不再出现 `mf_remove_natives`、`remove_natives`、`移除土著`、`去除土著`。

重点检查：
- `check_remove_natives_decision_removed` 必须通过。

## 幕府转移与异常层级

### 7. 外样大名独立战争夺取京都

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_shogunate_transfer_and_layer_cleanup` 的幕府转移部分。
- 我输入：`A` 成为新幕府。
- 我输入：旧幕府直属大名 `B` 转挂到 `A` 下。
- 我输入：保留 `B` 的合法陪臣 `C`。

预期：
- `A` 获得 `shogunate`。
- `B` 的宗主变为 `A`。
- `C` 的宗主仍是 `B`。
- 合法陪臣不会被新幕府整理拉平成幕府直属。

重点检查：
- `test_shogunate_transfer_and_layer_cleanup` 必须通过。

### 8. 新幕府继承旧幕府体系时的在战争大名

步骤：
- 我待补函数：`test_shogunate_transfer_keeps_warring_daimyo_pending`。
- 我需要给模拟器增加战争状态字段。
- 我输入：`A` 成为新幕府、`B` 不在战争、`C` 仍与新幕府交战。

预期：
- `B` 转挂到新幕府。
- `C` 不被强行吞并。
- `C` 被标记为后续收服目标或保留待处理状态。

重点检查：
- 当前未覆盖战争状态，不把本条作为当前通过标准。

### 9. 幕府直属陪臣保持合法

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_shogunate_transfer_and_layer_cleanup` 的幕府直属陪臣部分。
- 我输入：把 `B` 设置成 `SHO` 的 `mf_retainer_daimyo_vassal`。
- 我输入：执行属国层级整理入口。

预期：
- `B` 仍是 `SHO` 的 `mf_retainer_daimyo_vassal`。
- `B` 拥有 `daimyo` 政府改革。

重点检查：
- 幕府属于幕府体制内国家，可以直接作为陪臣大名的宗主。

### 10. 非幕府大名不能直接拥有谱代大名

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我输入：执行 `test_shogunate_transfer_and_layer_cleanup` 的非法谱代清理部分。
- 我输入：把 `B` 设置成 `A` 的 `mf_daimyo_vassal`。
- 我输入：执行非法谱代清理入口。

预期：
- `B` 变为 `A` 的 `mf_retainer_daimyo_vassal`。
- `B` 拥有 `daimyo` 政府改革。

重点检查：
- 非幕府国家不能直接保留谱代属国。

### 10A. 陪臣大名第一阶政府改革保持稳定

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我运行 `python tools/check_static_regressions.py`。
- 我输入：建立幕府 `SHO`、谱代大名 `A` 和陪臣大名 `B`；`A` 是 `SHO` 的 `mf_daimyo_vassal`，第一阶改革是 `mf_daimyo`。
- 我输入：把 `B` 设为 `A` 的 `mf_retainer_daimyo_vassal`，第一阶政府改革设为 `daimyo`。
- 我输入：另建真正独立大名 `I`、朝贡大名 `T` 和陪臣 `C`；`I` 与 `T` 的第一阶改革是 `indep_daimyo`，`T` 只属于 `tributary_state`，`C` 是 `I` 的 `mf_retainer_daimyo_vassal` 且第一阶改革是 `daimyo`。
- 我输入：把 `D` 设为幕府 `SHO` 的 `mf_retainer_daimyo_vassal`，第一阶改革设为 `daimyo`。
- 我输入：把持有 `indep_daimyo` 的 `V` 设为普通 `vassal`。
- 我输入：连续执行多轮属国状态刷新、政府改革有效性检查和 `mf_normalize_daimyo_first_layer_reform_effect`。
- 我查询：每轮刷新后 `B`、`C`、`D`、`I`、`T` 和 `V` 的第一阶政府改革。

预期：
- 每轮查询 `B` 的属国类型，始终返回 `mf_retainer_daimyo_vassal`。
- 每轮查询 `B` 的第一阶政府改革，始终返回 `daimyo`。
- 每轮查询 `C` 的属国类型和第一阶政府改革，也始终返回 `mf_retainer_daimyo_vassal` 与 `daimyo`。
- 每轮查询幕府直属陪臣 `D`，始终返回 `mf_retainer_daimyo_vassal` 与 `daimyo`。
- 每轮查询无宗主的 `I` 和仅为朝贡国的 `T`，始终返回 `indep_daimyo`。
- 查询普通属国 `V`，返回不能保持 `indep_daimyo`。
- 各国不会在 `daimyo` 与 `indep_daimyo` 之间交替。

重点检查：
- `test_retainer_daimyo_reform_stability` 必须通过。
- 修复必须让 `daimyo` 对陪臣属国本身保持有效，不能依靠月脉冲反复删除、重加改革掩盖失效。
- 谱代大名只允许幕府作为宗主；陪臣大名允许任何 `sort_of_mf = yes` 的国家作为宗主。
- 独立大名只能无宗主或属于 `tributary_state`，自定义国家分支也不能绕过该限制。
- 规范化 effect 必须按属国类型分别添加 `mf_daimyo`、`daimyo` 或 `indep_daimyo`，不能再把独立大名统一改成 `daimyo`。

### 10B. 删除原版独立大名兜底事件

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我运行 `python tools/check_static_regressions.py`。
- 我输入：建立仍持有 `daimyo` 改革、已经真正无宗主的大名 `A`。
- 我查询：模组覆盖文件中是否仍定义 `flavor_jap.57`。
- 我输入：对 `A` 执行 `mf_normalize_daimyo_first_layer_reform_effect`。
- 我查询：`A` 的第一阶政府改革。

预期：
- 模组覆盖文件中不存在 `flavor_jap.57` 事件定义。
- 原版事件不会再抢先把转挂中的大名改成独立大名，也不会每月重复触发。
- `A` 由本模组规范化 effect 转成 `indep_daimyo`；第 1、2 条场景中的属国则继续按实际属国类型获得对应大名改革。

重点检查：
- `test_independent_daimyo_normalization_without_flavor_jap_57` 必须通过。
- `check_flavor_jap_57_is_removed` 必须确认模组覆盖文件中没有该事件定义，不能恢复空壳事件或逐项排除属国类型。
- 独立大名转换由本模组的身份规范化链路负责，不能依赖原版每月兜底事件。

## 虾夷征讨与外部目标

### 11. 虾夷征讨完整流程

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我运行 `python tools/check_static_regressions.py`。
- 我输入：读取 `shogun_weixin.i` 的按钮条件。
- 我查询：按钮条件是否直接进入 `japan_region` 省份作用域。
- 我查询：省份作用域是否用 `NOT = { area = hokkaido_area }` 排除北海道区域。
- 我查询：剩余省份是否要求 `owned_by = ROOT`。
- 我查询：按钮条件是否仍包含 `type = all`、北海道省份 ID、额外 `OR` 或属国控制判断。
- 我查询：按钮条件是否没有调用 `mf_shogun_controls_japan_region_except_hokkaido`。
- 我查询：按钮条件是否没有 `is_empty = yes`。
- 我输入：执行 `test_xiayi_campaign`。
- 我输入：建立体系外国家 `XIA`。
- 我输入：把 `XIA` 设为当前虾夷目标。
- 我输入：检查虾夷 CB 是否可用。
- 我输入：执行收服虾夷和平效果。

预期：
- 查询 `shogun_weixin.i`，返回 `japan_region = { NOT = { area = hokkaido_area } owned_by = ROOT }`。
- 查询 `shogun_weixin.i`，返回不包含 `type = all`、`OR`、北海道省份 ID、属国控制判断、`mf_shogun_controls_japan_region_except_hokkaido`、`mf_shogun_system_holds_province` 和 `is_empty = yes`。
- 查询 AI 维新九决议，返回与按钮一样直接检查北海道以外日本地区。
- 当前 `xiayi` 指向 `XIA`。
- `XIA` 拥有 `mf_xiayi_target` 时，虾夷 CB 可用。
- 收服后，`XIA` 的宗主是 `SHO`。
- 收服后，`XIA` 是 `daimyo_vassal`。
- 收服后，`XIA` 不再拥有 `mf_xiayi_target`。
- 收服后，虾夷 CB 不再可用。

重点检查：
- `test_xiayi_campaign` 必须通过。
- `check_static_regressions.py` 必须确认 `japan_region` 进入省份作用域后，直接排除北海道区域并检查 `owned_by = ROOT`。
- `check_static_regressions.py` 必须确认 CB 进攻方只锁当前将军，目标方用 `FROM` 锁当前 `xiayi`。

### 12. 大名体系国家不能被设为虾夷

步骤：
- 我待补函数：`test_sort_of_mf_cannot_be_xiayi_target`。
- 我输入：建立 `daimyo_vassal`、`mf_daimyo_vassal`、`mf_retainer_daimyo_vassal` 和 `indep_daimyo`。
- 我输入：分别尝试设为虾夷目标。

预期：
- 大名体系国家不能成为有效虾夷目标。
- 不能因此获得虾夷 CB。

重点检查：
- 我当前只覆盖体系外 `XIA`，本条待补。

## 参勤与御令

### 13. 参勤交代费用、拒绝与正式改革

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我运行 `python tools/check_static_regressions.py`。
- 我输入：以大名总发展度 `60`、首都到幕府首都的距离 `0`、`12`、`24`、`48` 和年度收入 `120` 计算参勤费用。
- 我查询：每组的距离倍率、固定金钱、年收入扣除和总扣款。
- 我输入：分别设置独立倾向 `50`、贷款 `5`笔、贷款 `10`笔及多个条件同时命中。
- 我查询：参加与拒绝的 AI 权重，以及由权重计算出的拒绝率。
- 我输入：直属大名和下一层陪臣接受参勤；另一个大名拒绝参勤。
- 我查询：接受者的金钱、拒绝者威望、持续一年的拒绝修正和幕府 `shogun_value`。
- 我查询：拒绝者是否获得持久拒参 flag，幕府是否获得参勤讨伐理由，是否触发强制履行或强化惩戒入口。
- 我输入：完成 `shogun_weixin.h`，再执行一次年度参勤扫描，查询参勤临时状态、幕府常驻修正、`cb_sengoku` 和大名结盟条件。

预期：
- 距离系数分别返回 `0`、`1`、`2`、`3`，永不超过 `3`。
- 固定扣款均返回 `12`，年度扣款分别返回 `0`、`40`、`80`、`120`，总扣款分别返回 `12`、`52`、`92`、`132`。
- 固定扣款返回 `总发展度 / 5`，年度扣款返回 `年度收入 / 3 × min(首都距离 / 12, 3)`，总扣款返回两者之和。
- 无附加条件时参加和拒绝均返回权重 `100`，拒绝率为 `50%`。
- 独立倾向达到 `50`时参加权重返回 `20`，拒绝率为 `83.33%`；贷款达到 `5`笔时参加权重返回 `10`，拒绝率为 `90.91%`；贷款达到 `10`笔时参加权重返回 `0`，必定拒绝。
- 贷款达到 `5`笔且独立倾向达到 `50`时，参加权重依次乘以 `0.1`和 `0.2`后返回 `2`，拒绝率为 `98.04%`。
- 拒绝者威望增加 `10`，幕府统御减少 `0.5`。
- 拒绝者只获得持续一年的 `mf_sankin_kotai_refused`；不设置持久拒参 flag，幕府不获得参勤讨伐理由，也不触发强制履行或强化惩戒。
- 完成改革五后不再发送参勤事件，应仁之乱内战 CB 不可用。
- 完成改革五后结束直属大名和下一层陪臣仍在生效的参勤旅行与拒绝临时修正。
- 幕府获得 `jiujizhengshi_2`，属国发展度造成的独立倾向降低 `20%`。
- 同一幕府体系的大名不能建立新同盟，改革完成前已有的大名同盟被解除；体系外国家不受该幕府改革影响。

重点检查：
- 年度入口只遍历幕府直属大名和下一层陪臣，不扫描全世界。
- 事件使用 `export_to_variable` 直接取得大名的 `total_development`、`years_of_income` 和大名与幕府的 `capital_distance`，不能保留逐档判断。
- 距离目标是 `event_target:zhengyidajiangjun`，陪臣不能错误地对直接宗主计算距离。
- 事件只保留一个参加选项和一个拒绝选项；参加基础权重为 `100`并承载全部条件系数，拒绝基础权重固定为 `100`，不得按条件复制拒绝选项。

### 13A. 亲藩与谱代是同一身份

步骤：
- 我输入：建立幕府 `SHO` 和三个直属普通大名，使其中两个对 `SHO` 关系达到 `150`。
- 我运行静态检查，查询 GUI 按钮和 AI 决议中的关系条件，以及改革五之后的改革按钮、滚轮和大名盾徽定义。
- 我输入：完成 `shogun_weixin.e` 并对第三个大名执行 `mf_convert_daimyo`。
- 我查询：目标属国类型、政府改革与宗主。

预期：
- 关系条件返回合法触发器 `has_opinion`，不能出现会中断文件解析的裸 `opinion = {`。
- `shogun_weixin.f` 至 `shogun_weixin.l`、维新滚轮和大名盾徽定义仍能在改革五之后注册。
- 关系门槛只计算幕府直属大名。
- 目标返回 `mf_daimyo_vassal`、`mf_daimyo` 和 `SHO`。

重点检查：
- 不能只检查花括号平衡；触发器合法但作用域错误和触发器名称无效是两类不同回归。
- GUI 条件和 AI 决议必须保持同一写法，避免玩家按钮正常而 AI 决议持续写入错误日志。
- 已经通过 `tag = event_target:zhengyidajiangjun` 确认当前国家就是幕府时，后续国家条件直接使用默认作用域，不重复进入同一个事件目标。

### 13B. 幕府省份标记与归还

步骤：
- 我输入：幕府执行批量登记决议，把幕府、直属大名和下一层陪臣拥有的未登记省份标记为幕府省份。
- 我输入：体系外国家 `OUT` 获得 `a1`，然后接受幕府归还要求。
- 我输入：为 `a1` 同时设置现存幕府体系核心国和已灭亡核心国。
- 我查询：省份标记、最终所有者和恢复国家的宗主。

预期：
- 省份标记在所有者变化后仍然存在。
- 接受归还后优先回到现存幕府体系核心国；没有现存体系核心国时，恢复的旧国成为幕府直属 `daimyo_vassal`。

### 13C. 转让非法属国

步骤：
- 我输入：直属大名 `A` 拥有陪臣大名 `B` 和另一个下级属国 `C`。
- 我输入：幕府在外交界面直接选择 `B`，执行解放属国行动。
- 我查询：`B` 的宗主、属国类型、政府改革和幕府统御。

预期：
- 每次只处理幕府明确选择的一个目标，不随机选择 `B` 或 `C`。
- 合法陪臣也能被选择；`B` 返回宗主 `SHO`、属国类型 `daimyo_vassal`、改革 `daimyo`。
- 基础接受分支的幕府统御增加 `2`。

### 13D. 绝罚与主动宣战限制

步骤：
- 我输入：幕府绝罚已拒令的大名 `A`。
- 我查询：`A` 能否主动宣战、能否被宣战、属国类型和幕府统御。

预期：
- `A` 在绝罚期间不能主动宣战，但不变更属国类型或省份。
- 幕府统御增加 `2`。

### 13E. 武家诸法只禁止内部私战

步骤：
- 我输入：完成 `shogun_weixin.f`，让直属大名、谱代和陪臣分别尝试对体系内国家、体系外国家和自己宗主宣战。
- 我查询：每次 `declarewar` 条件的返回值。

预期：
- 对同一幕府体系的非宗主国家返回否。
- 对体系外国家返回是。
- 对宗主发动独立战争返回是。

### 13F. 原版语法与错误日志交叉检查

步骤：
- 我查询原版 `common/event_modifiers` 中年度陆军传统修正的真实 key。
- 我查询原版 `common/subject_types/00_subject_types.txt` 中 `modifier_subject.trigger` 和 `is_potential_overlord` 的实际写法。
- 我查询 `topbar.gui` 中每个 `scripted = yes` 元素的界面类型，并与 `custom_gui` 中的定义类型双向逐项对应。
- 我查询 `shogun_button_gui_window`、`shogun_gui_window` 和 `enable_shogun_gui` 的入口、主窗口与开关 flag 链路。
- 我运行静态检查，并读取最新 `error.log` 中属于本模组脚本文件的解析错误、未知触发器、未知效果和未知修正。

预期：
- `mf_ezo_campaign_preparation` 使用原版事件修正中的 `army_tradition = 1`，不使用无效的 `yearly_army_tradition`。
- `mf_subject_types.txt` 在 `subject_types` 加载阶段只使用该阶段能够识别的原生条件，不产生 `mf_daimyo_foreign_learning_range` 或 `sort_of_mf` 未知触发器错误。
- `windowType`、`instantTextBoxType` 和 `iconType` 分别返回 `custom_window`、`custom_text_box` 和 `custom_icon` 的同名定义；`guiButtonType` 返回 `custom_button` 或 `custom_shield` 的同名定义。反向查询每个 `custom_*` 定义，也必须返回带直属 `scripted = yes` 的同名 GUI 控件。
- 幕府入口窗口的 `potential` 按移植来源使用 `ROOT = { has_discovered = 1818 }` 取得玩家国家；按钮本身仍在默认国家作用域直接判断。点击入口后设置 `shogun_gui_window_open`，主窗口以该 flag 控制显示。
- `mf_shogun_ai.1` 像原版国家事件一样定义事件图片，不产生 `is missing picture` 警告。
- 维新 GUI、AI 决议、外交行动、事件和属国类型不产生新的脚本解析错误。

重点检查：
- 原版实例和游戏实际生成的 `error.log` 必须交叉验证，不能仅凭静态花括号检查判断通过。
- `shogun_gui_bg` 和 `shogun_gui_dip_icon` 必须像正常来源模组一样保留 `scripted = yes`，不能留下没有 GUI 实例绑定的 `custom_icon`。
- `custom_window` 的窗口可见性作用域与 `custom_button` 的按钮作用域不能混用同一条简化规则；入口窗口必须与已验证的移植来源保持一致。
- 本地化编码提示不属于脚本语法修复范围，不能由 Codex 执行编码转换。

### 13G. 大名盾徽刷新不污染月脉冲日志

步骤：
- 我输入：建立幕府 `SHO` 和少于 30 个大名，只保存实际存在的大名盾徽全局事件目标。
- 我运行静态检查，查询 `on_monthly_pulse`、`bl_shogun_refresh` 和大名盾徽目标清理 effect。
- 我查询：月脉冲是否仍调用 `bl_shogun_refresh`。
- 我查询：`shogun_daming_1` 至 `shogun_daming_30` 是否都只在对应全局事件目标存在时清理。

预期：
- 月脉冲返回不调用 `bl_shogun_refresh`；打开面板、刷新按钮、开局和幕府体系变化入口仍可刷新盾徽。
- 每个大名盾徽目标都先返回 `has_saved_global_event_target = shogun_daming_N`，再执行对应的 `clear_global_event_target`。
- 新 `error.log` 不再反复出现 `Trying to remove undefined event target: shogun_daming_N`。

重点检查：
- 不用隐藏月脉冲维持纯 UI 名单；玩家打开面板或点击刷新时再重建名单。
- 不能为了消除日志而取消清理，否则大名减少后会残留旧盾徽。

## 工具检查与最低回归

### 14. 静态回归检查

步骤：
- 我运行 `python tools/check_static_regressions.py`。

预期：
- 所有静态检查输出 `PASS`。

重点检查：
- 当前覆盖：土著移除决议不存在、年度参勤入口受御令限制、模组覆盖文件删除 `flavor_jap.57`、大名私战收服不增加幕府统御。

### 15. 本地化显示检查

步骤：
- 我待补工具：`tools/check_localization_keys.py`。
- 我需要让工具扫描本次改动涉及的事件、外交行动、CB、战争目标、和平条款、修正和 tooltip key。
- 我需要让工具在 `docs/本地化中文备份` 和 `localisation` 中确认对应 key 存在。

预期：
- 玩家可见 key 均能在本地化文件中找到。
- 不把“进游戏看有没有裸 key”作为当前自动测试。

重点检查：
- 不执行本地化编码转换。
- 只补中文文本，不改转码流程。

### 16. 错误日志检查

步骤：
- 我待补工具：`tools/check_error_log_regressions.py`。
- 我需要让工具读取 `Documents/Paradox Interactive/Europa Universalis IV/logs/error.log`。
- 我需要让工具搜索本次改动文件名、事件 id、CB key、和平条款 key、tooltip key 和 `missing localization`。

预期：
- 如果存在 `error.log`，工具能列出相关新增错误。
- 如果不存在 `error.log`，工具明确报告无法覆盖，不视为通过。

重点检查：
- 不能把“手动打开 error.log”写成测试步骤。

### 17. 推送前最低检查

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我运行 `python tools/check_static_regressions.py`。
- 我修改任务坐标时运行任务布局工具。
- 我改动本地化时，运行待补本地化 key 工具。
- 我改动事件、CB、和平条款或外交行动时，运行待补 error.log 工具。

预期：
- 已存在工具全部通过。
- 我还没实现待补工具时，在最终说明中明确该覆盖缺口。

重点检查：
- 不能用“进游戏抽测”替代工具缺口。

## 待实现或大改动专项模板

以下用例只在对应机制已有入口或本次改动触及时启用。我启用时必须先补函数或静态检查，再把本条移出待补状态。

### 18. 闭关锁国选择与解除

步骤：
- 我待补函数：`test_sakoku_enable_and_disable`。
- 我输入：增加闭关 flag、修正、外交入口限制字段。
- 我输入：执行闭关入口。
- 我输入：执行解除入口。

预期：
- 闭关后，闭关 flag 或修正存在。
- 幕府内部治理行动仍可用。
- 受限外交行动不可用。
- 解除后，闭关 flag、修正和限制消失。

重点检查：
- 我没有补模拟器函数前，不把本条作为回归通过标准。

### 19. 虾夷地区限制与解除

步骤：
- 我待补函数：`test_xiayi_region_limit_and_unlock`。
- 我输入：在 `test_xiayi_campaign` 基础上增加地区限制 flag 或修正。
- 我输入：执行限制入口、解除入口、目标被收服三个分支。

预期：
- 限制只作用于当前虾夷目标。
- 解除后入口状态改变。
- 目标成为幕府直属大名后，限制被清除。

重点检查：
- 不破坏第 11 条虾夷 CB 和收服逻辑。

### 20. 正式改革停止参勤事件链

步骤：
- 我运行 `python tools/mf_shogun_logic_simulator.py`。
- 我运行 `python tools/check_static_regressions.py`。
- 我输入：直属大名和下一层陪臣分别参加、拒绝参勤，然后完成 `shogun_weixin.h`。
- 我查询：拒绝当次的威望、临时修正和幕府统御变化，以及年度参勤目标、幕府修正及大名结盟条件。
- 我查询：拒绝效果、事件、外交行动、任务和绝罚条件中是否存在拒参 flag、参勤讨伐理由、强制参勤或强化惩戒链。

预期：
- 拒绝参勤后，目标威望增加 `10`并获得持续一年的 `mf_sankin_kotai_refused`，幕府统御减少 `0.5`。
- 拒绝效果不设置持久拒参 flag，不授予任何参勤讨伐理由，也不触发后续幕府提示或惩戒事件。
- 完成正式改革后，年度参勤目标返回空，仍在生效的参勤旅行与拒绝临时修正结束。
- 幕府获得降低属国发展度独立倾向的 `jiujizhengshi_2`。
- 同一幕府体系的大名结盟返回否，体系外国家之间的结盟不受影响。
- 返回不存在 `mf_refused_sankin_kotai`、拒参授予 `cb_disloyal_vassal`、`mf_enforce_sankin_kotai`、`mf_shogun_forced_sankin_kotai`、`mf_shogun_sankin_punishment_upgraded` 和 `mf_shogun_punish_sankin_refusal` 的功能定义或调用。

重点检查：
- 玩家按钮与 AI 决议必须调用同一个正式改革 effect，不能只让玩家分支停止事件链。
- 禁止结盟应写在原版 `allianceaction` 入口，不能靠月脉冲反复拆盟。

### 21. 绝罚惩戒链路

步骤：
- 我待补函数：`test_excommunication_chain`。
- 我输入：增加绝罚入口、合法目标状态、惩罚修正、CB 和冷却。

预期：
- 合法目标获得绝罚惩罚。
- 非法目标不可用。
- 绝罚不改变目标属国类型、政府改革或省份归属。

重点检查：
- 绝罚只能惩戒，不应承担转挂或吞并逻辑。

### 22. 武家诸法禁止私战

步骤：
- 复用 `test_lecture_grace_enforce_partition_executor` 的强制和平分支。
- 我待补函数：`test_buke_shohatto_unlocks_enforce_peace`。
- 我输入：增加任务解锁状态。

预期：
- 未解锁前，强制和平入口不可用。
- 解锁后，强制和平接受和拒绝分支按第 6B 条执行。

重点检查：
- 禁止私战本身不改变属国层级。

### 23. 归还非法领土完整链路

步骤：
- 复用 `test_return_land_accept_and_decline`。
- 我待补函数：`test_return_land_target_selection`。
- 我输入：增加旧家核心、非法领土、省份区域和候选排序字段。

预期：
- 接受时恢复正确旧家。
- 拒绝时添加非法领地修正和归还 CB。
- 不依赖 `previous_owner`。
- 非法领土修正和临时目标能被清理。

重点检查：
- 当前已覆盖接受和拒绝结果，待补目标选择算法。

### 24. 一国一主与禁止属国

步骤：
- 我待补函数：`test_one_lord_one_house_subject_limits`。
- 我输入：增加“一国一主”状态。
- 我输入：尝试创建普通属国、陪臣大名、幕府合法分封。

预期：
- 被禁止入口不可用。
- 幕府合法分封、归还领地、虾夷收服仍可用。
- 非幕府国家不被清理。

重点检查：
- 不回滚第 1、2、7、9、10 条层级修复。

### 25. 谱代上限升级

步骤：
- 我待补函数：`test_fudai_limit_and_upgrade`。
- 我输入：增加谱代上限、已用名额、升级来源。
- 我输入：连续创建谱代直到上限。
- 我输入：执行升级后再次创建谱代。

预期：
- 达到上限后不能任命。
- 升级后上限提高。
- 谱代减少后释放名额。
- 新幕府成立后按当前幕府重新计算。

重点检查：
- 上限规则不能散落在多个入口中。

### 26. 任务树并行线完成条件

步骤：
- 我运行任务布局工具。
- 我待补静态工具：`tools/check_mission_dependencies.py`。
- 我需要让工具读取 `missions/mf_shogun_missions.txt`。
- 我需要让工具检查 `required_missions`、`trigger`、`effect` 和任务坐标。

预期：
- 汇合任务必须列出全部前置。
- 坐标不与既有任务重叠。
- 任务奖励 key 能被本地化工具覆盖。

重点检查：
- 不把人工查看连线作为当前测试标准。
